version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./:/app
    environment:
      - NODE_ENV=production
      - JWT_SECRET=change_this_secret
    restart: unless-stopped